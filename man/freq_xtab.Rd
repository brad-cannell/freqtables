% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freq_xtab.R
\name{freq_xtab}
\alias{freq_xtab}
\title{Create Contingency Tables (Cross-Tabulations)}
\usage{
freq_xtab(.data, .row, .col, margins = TRUE)
}
\arguments{
\item{.data}{A data frame or grouped data frame.}

\item{.row}{An unquoted column name for the row variable (e.g., exposure).}

\item{.col}{An unquoted column name for the column variable (e.g., outcome).}

\item{margins}{If TRUE (default), marginal totals ("Sum") are appended to
the rows and columns of the table.}
}
\value{
For ungrouped data, a matrix with row and column names set to the
  variable levels, with marginal totals if \code{margins = TRUE}. For
  grouped data, a named list of such matrices (one per group level).
}
\description{
The freq_xtab function creates a contingency table (matrix)
  from two categorical variables. With \code{dplyr::group_by()}, it creates
  an array of contingency tables (one per group level).
}
\examples{
library(dplyr)
library(freqtables)

data(mtcars)

# Simple contingency table
mtcars \%>\%
  freq_xtab(am, cyl)

# Grouped contingency tables
mtcars \%>\%
  group_by(vs) \%>\%
  freq_xtab(am, cyl)
}
