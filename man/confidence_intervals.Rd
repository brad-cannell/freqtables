% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_helpers.R
\name{confidence_intervals}
\alias{confidence_intervals}
\alias{wald_ci}
\alias{logit_ci}
\title{Add confidence intervals to frequency counts}
\usage{
wald_ci(.data, percent_ci = 95, se = FALSE, critical_value = FALSE)

logit_ci(.data, percent_ci = 95, se = FALSE, critical_value = FALSE)
}
\arguments{
\item{.data}{A frequency table from \code{\link[=freq_tbl]{freq_tbl()}} or \code{\link[=freq_table]{freq_table()}}.
A plain data frame is also accepted if it contains \code{n} and
\code{n_group} with \code{prop_group}/\code{percent_group}, or \code{n_total} with
\code{prop}/\code{percent}. Optional overall columns use
\code{n_total} with \code{prop_total}/\code{percent_total}.
An ungrouped compact table needs its original freqtables metadata or an
explicit \code{n_total}; totals are never reconstructed by summing a possibly
filtered table. Counts must be finite nonnegative integers no larger than
their denominators. Displayed proportions must agree with those counts.}

\item{percent_ci}{Confidence level as a percentage, strictly between 0 and
100; default 95. Independent of the table's proportion/percentage scale.}

\item{se}{Logical. Include standard errors? Default \code{FALSE}.}

\item{critical_value}{Logical. Include Student-t critical values in
\code{t_crit}, \code{t_crit_group}, and/or \code{t_crit_total}? Default \code{FALSE}.}
}
\value{
The input table with \code{lcl}/\code{ucl}, \code{lcl_group}/\code{ucl_group},
and/or \code{lcl_total}/\code{ucl_total} for each proportion present.
Bounds and standard errors use the input scale; critical values do not
scale. Repeated calls replace interval/detail columns for the same
proportions, including removal of unrequested optional detail columns.
}
\description{
These helpers add intervals to \code{\link[=freq_tbl]{freq_tbl()}} output, and are also used by
\code{\link[=freq_table]{freq_table()}}. Both retain the package's Student-t convention:
\code{SE = sqrt(p * (1 - p) / (N - 1))}, with \code{N - 1} degrees of freedom.
For within-group intervals, N is that group's denominator; for overall
intervals, N is the total input size. This is a compatibility convention,
not the usual normal-z binomial Wald interval.
}
\details{
\code{wald_ci()} returns \verb{p +/- t * SE} without clipping to the unit interval.
\code{logit_ci()} transforms \verb{log(p / (1 - p)) +/- t * SE / (p * (1 - p))}
back to the proportion scale with \code{\link[stats:Logistic]{stats::plogis()}}. At p = 0 or 1,
logit bounds are missing (no continuity correction); Wald bounds equal p.
With N <= 1, SE, critical values, and both types of bounds are missing.
}
\examples{
freq_tbl(mtcars, am) |> wald_ci()
freq_tbl(mtcars, am) |> logit_ci(percent_ci = 99, se = TRUE)
mtcars |> dplyr::group_by(cyl) |>
  freq_tbl(am, percent = TRUE, overall = TRUE) |>
  logit_ci(critical_value = TRUE)
}
