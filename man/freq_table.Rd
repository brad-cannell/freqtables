% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freq_table.R
\name{freq_table}
\alias{freq_table}
\title{Frequency tables with confidence intervals}
\usage{
freq_table(
  .data,
  .freq_var,
  ...,
  percent_ci = 95,
  ci_type = "logit",
  drop = FALSE,
  percent = FALSE,
  overall = FALSE,
  se = FALSE,
  critical_value = FALSE,
  generic_col_names = FALSE
)
}
\arguments{
\item{.data}{A local data frame or grouped data frame. Rowwise data is not
supported; call \code{\link[dplyr:group_by]{dplyr::ungroup()}} first.}

\item{.freq_var}{One unquoted column name (or a string naming a column).
In a wrapper function, use \code{{{ variable }}}.}

\item{...}{Reserved for detecting obsolete multiple-outcome calls and
misspelled options. Must be empty; options must be named.}

\item{percent_ci}{Confidence level as a percentage, strictly between 0 and
100; default 95. Independent of the table's proportion/percentage scale.}

\item{ci_type}{Interval method: \code{"logit"} (default) or \code{"wald"}.
Both methods support ungrouped and grouped data. Supply a string.}

\item{drop}{Logical. If \code{FALSE} (default), retain unobserved factor levels
and empty factor groups with zero counts. If \code{TRUE}, retain observed
combinations only. Does not remove missing values.}

\item{percent}{Logical. If \code{FALSE}, return proportions in \code{prop},
\code{prop_group}, and (when requested) \code{prop_total}. If \code{TRUE}, use
\code{percent}, \code{percent_group}, and \code{percent_total} on the 0--100 scale.}

\item{overall}{Logical. For grouped data, append \code{n_total} and
\code{prop_total} (or \code{percent_total}): the cell count divided by the total
input rows, not the outcome's marginal proportion. For ungrouped data,
append only \code{n_total}; \code{prop} is already the overall proportion.}

\item{se}{Logical. Include standard errors? Default \code{FALSE}.}

\item{critical_value}{Logical. Include Student-t critical values in
\code{t_crit}, \code{t_crit_group}, and/or \code{t_crit_total}? Default \code{FALSE}.}

\item{generic_col_names}{Logical. If \code{TRUE}, replace each group column
with \code{group_01_col}/\code{group_01_cat}, etc., and the outcome with
\code{col}/\code{cat}. Category columns become character for stacking tables.}
}
\value{
An ungrouped tibble with the \code{\link[=freq_tbl]{freq_tbl()}} columns plus confidence
limits. Classes are \code{freq_table_one_way} (no groups),
\code{freq_table_two_way} (one group), or \code{freq_table_n_way} (multiple
groups), together with \code{freq_tbl} and tibble classes.
\code{\link[=freq_test]{freq_test()}} supports the first two cases; multiple grouping variables
do not imply a particular statistical test.
}
\description{
Combine \code{\link[=freq_tbl]{freq_tbl()}} with \code{\link[=logit_ci]{logit_ci()}} or \code{\link[=wald_ci]{wald_ci()}}. Accept one outcome;
supply groups using \code{\link[dplyr:group_by]{dplyr::group_by()}}. Defaults are compact proportions
with confidence intervals. See \code{vignette("migration")} for the transition
from the old percentage-scale, two-variable interface.
}
\examples{
freq_table(mtcars, am)
mtcars |> dplyr::group_by(cyl) |> freq_table(am)
mtcars |> dplyr::group_by(cyl, vs) |>
  freq_table(am, ci_type = "wald", percent_ci = 90)
freq_table(mtcars, am, percent = TRUE, overall = TRUE,
           se = TRUE, critical_value = TRUE, generic_col_names = TRUE)
}
