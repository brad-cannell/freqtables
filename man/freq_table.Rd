% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freq_table.R
\name{freq_table}
\alias{freq_table}
\title{Frequency table with confidence intervals}
\usage{
freq_table(
  .data,
  ...,
  percent_ci = 95,
  ci_type = "logit",
  drop = FALSE,
  percent = FALSE,
  overall = FALSE,
  generic_col_names = FALSE,
  se = FALSE,
  critical_value = FALSE,
  digits = 2
)
}
\arguments{
\item{.data}{A data frame (optionally grouped with dplyr::group_by()).}

\item{...}{A single categorical outcome variable.}

\item{percent_ci}{Confidence level as a percent (default 95).}

\item{ci_type}{Either "logit" (default) or "wald".}

\item{drop}{Passed to dplyr::count(.drop = ).}

\item{percent}{If TRUE, express proportion-related columns as percentages and
rename `prop*` columns to `percent*`.}

\item{overall}{If TRUE, include overall totals/proportions.}

\item{generic_col_names}{If TRUE, return generic variable/category columns.}

\item{se}{If TRUE, retain standard error columns in output.}

\item{critical_value}{If TRUE, retain critical value columns in output.}

\item{digits}{Number of decimal places displayed for `prop*`/`percent*` and
`lcl*`/`ucl*` columns. Must be a non-negative integer.}
}
\value{
A tibble.
}
\description{
`freq_table()` is a higher-level wrapper around `freq_tbl()` that adds
confidence intervals by default.
}
