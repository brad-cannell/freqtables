% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freq_tables.R
\name{freq_tables}
\alias{freq_tables}
\title{Create Multiple Frequency Tables at Once}
\usage{
freq_tables(.data, ..., percent_ci = 95, ci_type = "logit", drop = FALSE)
}
\arguments{
\item{.data}{A data frame or grouped data frame.}

\item{...}{One or more unquoted column names to tabulate.}

\item{percent_ci}{The confidence level as a percentage. Default is 95.}

\item{ci_type}{Method for confidence interval estimation. Either "logit"
(default) or "wald".}

\item{drop}{If FALSE (default), unobserved factor levels will appear in
the results with n = 0.}
}
\value{
A tibble with columns \code{col} (the variable name) and
  \code{freq_table} (a list column of frequency table data frames).
}
\description{
The freq_tables function (with an "s") allows the user to pass
  more than one outcome variable. It returns a nested data frame with one
  row per variable and a list column containing the individual frequency
  tables.
}
\examples{
library(dplyr)
library(freqtables)

data(mtcars)

# Multiple one-way tables
mtcars \%>\%
  freq_tables(am, cyl)

# Multiple grouped tables
mtcars \%>\%
  group_by(vs) \%>\%
  freq_tables(am, cyl)
}
